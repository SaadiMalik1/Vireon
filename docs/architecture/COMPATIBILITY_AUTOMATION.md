# Compatibility Automation

The `workspace` repo contains a matrix CI test that clones `vireon`, `vireon-lab`, and `neurodsl`, building them together to ensure no breaking changes bypass the contract tests.
