# TWO-REPO BOUNDARY SPECIFICATION\n\nExactly what goes where, and why.\n\n---\n\n## Vireon/ — Production Runtime\n\n**Audience**: BCI company engineers, security researchers, regulatory reviewers, integration testers.\n\n**Mandate**: This is the thing you `pip install vireon` and use as a library. It has an API, a CLI, and a Docker image. It does NOT have a dashboard, tutorials, or educational content.\n\n### Directory Structure\n\n```\nVireon/\n├── crates/                          # Rust code\n│   ├── neurodsl/                    # NeuroDSL compiler + VM (3 crates)\n│   └── ringbuf/                     # SPSC ring buffer (new, ADR-015)\n├── src/\n│   └── vireon/                      # Python package\n│       ├── __init__.py              # Version, public API\n│       ├── __main__.py              # CLI (click)\n│       ├── sdk/                     # Public API surface\n│       │   ├── base_interfaces.py   # ITwin, IDetector, ICryptoChannel, ITransport\n│       │   ├── events.py            # Event, IEventBus\n│       │   ├── interfaces.py        # IProvider, IVireonPlugin, OrchestratorContext\n│       │   ├── manifest.py          # CapabilityManifest (enhanced with signatures)\n│       │   ├── native_provider.py   # C-ABI provider loader (ctypes)\n│       │   ├── plugin.py            # V2 plugin base\n│       │   ├── runner.py            # Sandbox isolation (bwrap, seccomp)\n│       │   ├── state.py             # IStateStore\n│       │   └── subprocess_provider.py # JSON-RPC provider\n│       ├── runtime/                 # Runtime engine\n│       │   ├── orchestrator.py      # V2 VireonOrchestrator (kernel)\n│       │   ├── scheduler.py         # Bifurcated clock (ADR-005)\n│       │   ├── clock.py             # Owned clock (ADR-004)\n│       │   ├── rng.py               # Owned RNG (ADR-004)\n│       │   ├── event_bus.py         # Async event bus (rewritten)\n│       │   ├── state_store.py       # CRDT-backed state store (ADR-008)\n│       │   ├── capability_engine.py # Real enforcement (ADR-003, ADR-006)\n│       │   ├── config.py            # ExperimentConfig\n│       │   ├── guardrails.py        # Neuroethics guardrails (G1-G8)\n│       │   ├── evidence/            # Evidence pipeline\n│       │   │   ├── pipeline.py      # Evidence engine\n│       │   │   ├── merkle.py        # Merkle tree (ADR-014)\n│       │   │   └── signing.py       # Ed25519 signatures\n│       │   ├── data_plane/          # High-throughput data (ADR-002)\n│       │   │   ├── shared_memory.py # Zero-copy shared memory\n│       │   │   ├── ring_buffer.py   # Python wrapper for Rust ringbuf\n│       │   │   └── schemas/         # FlatBuffer/.proto definitions\n│       │   └── services/            # V2 services\n│       │       ├── engine.py        # ReplayEngine (deterministic)\n│       │       ├── sbom.py          # SBOM generation\n│       │       └── grpc_server.py   # gRPC control plane (ADR-002)\n│       ├── validation/              # Validation artifacts\n│       │   └── artifacts.py         # ValidationArtifact, ThreatReport, ReplayPackage\n│       └── mcp_server.py            # MCP integration (optional dep)\n├── tests/                          # All tests\n│   ├── test_architecture.py        # Layer boundary enforcement\n│   ├── test_determinism.py         # ADR-004 tests\n│   ├── test_scheduler.py           # ADR-005 tests\n│   ├── test_capability_engine.py   # ADR-003, ADR-006 tests\n│   ├── test_data_plane.py          # ADR-002, ADR-007, ADR-015 tests\n│   ├── test_state_store.py         # ADR-008 tests\n│   ├── test_evidence.py            # ADR-014 tests\n│   ├── test_integration.py         # End-to-end provider lifecycle\n│   ├── test_crypto.py              # Cryptographic primitive tests\n│   └── ...                         # Other unit tests\n├── threat_models/                   # STRIDE threat models (YAML)\n├── docs/\n│   ├── adr/                        # All 15 ADRs\n│   ├── api/                        # API reference (mkdocstrings)\n│   ├── architecture.md             # Architecture overview\n│   └── integration.md              # Vendor integration guide\n├── docker/\n│   └── vireon.Dockerfile           # Production multi-stage build\n├── schemas/                        # .proto and .fbs files\n├── .github/\n│   ├── workflows/ci.yml            # CI pipeline\n│   ├── ISSUE_TEMPLATE/             # Bug + feature templates\n│   └── PULL_REQUEST_TEMPLATE.md\n├── CODEOWNERS\n├── CONTRIBUTING.md\n├── GOVERNANCE.md\n├── SECURITY.md\n├── CODE_OF_CONDUCT.md\n├── Makefile\n├── pyproject.toml                  # Single Python project\n├── Cargo.toml                      # Rust workspace root\n├── CITATION.cff\n├── README.md                       # Substantive, self-contained\n└── LICENSE\n```\n\n### What Vireon Does NOT Contain\n\n- No Streamlit dashboard\n- No tutorial content\n- No knowledge base\n- No dataset management tools\n- No report HTML templates\n- No \"getting started\" tutorial (link to vireon-lab instead)\n- No lab experiment templates\n- No example attack scenarios for education\n\n### Dependencies (pyproject.toml)\n\n```\n[project]\nname = \"vireon\"\ndependencies = [\n    \"pydantic>=2.8.0\",\n    \"click>=8.3.0\",\n    \"pyyaml>=6.0.0\",\n    \"numpy>=1.24.0\",\n    \"grpcio>=1.60.0\",          # ADR-002 control plane\n    \"flatbuffers>=23.0.0\",     # ADR-007 zero-copy\n]\n\n[project.optional-dependencies]\ncrypto = [\"cryptography>=48.0.1\"]  # For evidence signing\nmcp = [\"mcp>=1.0.0\"]                # For AI agent integration\nneurodsl = []                         # Built from crates/ via maturin\ndev = [\"pytest>=9.0.3\", \"pytest-asyncio>=0.23.8\", \"mypy\", \"ruff\"]\n```\n\n---\n\n## vireon-lab/ — Educational Platform\n\n**Audience**: Students, new researchers, educators, CTF participants.\n\n**Mandate**: This is the thing you `pip install vireon-lab`, run `make demo`, and see a neurosecurity simulation in your browser. It depends on `vireon` as a library and demonstrates its capabilities through examples, tutorials, and interactive content.\n\n### Directory Structure\n\n```\nvireon-lab/\n├── vireon_lab/\n│   ├── __init__.py\n│   ├── dashboard/\n│   │   └── app.py                # Streamlit dashboard\n│   ├── providers/                  # Educational reference providers\n│   │   ├── datasets/              # EDF, FIF, CSV, MNE, synthetic readers\n│   │   ├── clinical/              # DBS emulator, clinical evaluator, threat atlas\n│   │   ├── hardware/              # OpenBCI, Muse, Emotiv, PiEEG emulators\n│   │   ├── firmware/              # Cortex-M stub, QEMU HIL\n│   │   └── protocols/             # BLE emulator + attacks\n│   ├── scenarios/                  # Attack scenario modules\n│   │   ├── adversarial_ml.py      # FGSM, PGD, C&W\n│   │   ├── poisoning.py           # Label flipping, backdoor\n│   │   └── sca.py                 # Side-channel (DPA)\n│   ├── reports/                    # Report generation\n│   │   ├── generator.py           # HTML/PDF/MD reports\n│   │   └── web/                   # Tailwind dashboard\n│   └── tutorials/                  # Step-by-step tutorials (NEW)\n│       ├── 01_first_simulation.md\n│       ├── 02_custom_provider.md\n│       ├── 03_attack_analysis.md\n│       └── 04_deterministic_replay.md\n├── knowledge/                       # Educational content\n│   ├── neuroscience/               # Brain regions, EEG, DBS, neurons\n│   ├── neurosecurity/              # Threat models, attack surfaces\n│   ├── signal-processing/          # FFT, filtering\n│   ├── standards/                  # FDA, ISO, cybersecurity guidance\n│   ├── protocols/                  # BLE\n│   └── papers/                     # Classic papers, references\n├── datasets/                        # Dataset metadata + fetcher\n│   ├── metadata/                   # YAML profiles for 10+ datasets\n│   └── synthetic/                  # Synthetic data generators\n├── examples/                        # Working examples (NEW — all must run)\n│   ├── basic_experiment.toml\n│   ├── custom_plugin.py\n│   ├── ble_attack_demo.py\n│   └── dbs_closed_loop_demo.py\n├── experiments/                     # Pre-configured experiment templates\n│   ├── default.toml\n│   ├── noise_attack.toml\n│   └── clinical_simulation.toml\n├── tests/\n│   ├── test_adversarial_attacks.py\n│   ├── test_ble_emulator.py\n│   ├── test_dbs_emulator.py\n│   ├── test_clinical_risk.py\n│   ├── test_web_server.py\n│   └── ...                         # 27 existing test files\n├── docs/\n│   └── tutorials/                  # Tutorial documentation (mkdocs)\n├── .github/\n│   └── workflows/ci.yml            # vireon-lab CI (simpler than Vireon)\n├── Makefile\n├── pyproject.toml\n├── README.md                       # Screenshot + \"make demo\" + link to Vireon\n└── LICENSE\n```\n\n### What vireon-lab Does NOT Contain\n\n- No core runtime code (sdk/, runtime/, services/)\n- No capability engine\n- No scheduler\n- No evidence pipeline\n- No NeuroDSL compiler/VM\n- No gRPC server\n- No ADRs\n- No threat model YAML (the STIX data stays because it's reference material, not runtime code)\n\n### Dependencies (pyproject.toml)\n\n```\n[project]\nname = \"vireon-lab\"\ndependencies = [\n    \"vireon>=2.0.0\",            # Production runtime\n    \"streamlit>=1.54.0\",\n    \"pandas>=2.2.0\",\n    \"plotly>=5.18.0\",\n    \"numpy>=1.24.0\",\n    \"scipy>=1.11.0\",\n]\n\n[project.optional-dependencies]\nbrainflow = [\"brainflow>=5.0.0\"]     # Real hardware\nedf = [\"pyedflib>=0.1.30\"]             # EDF file reading\nmne = [\"mne>=1.6.0\"]                   # Advanced EEG analysis\nlsl = [\"pylsl>=1.16.2\"]                # Lab Streaming Layer\nble = [\"bleak>=0.21.0\"]                # Real BLE devices\npdf = [\"weasyprint>=68.0\"]             # PDF report generation\ndev = [\"pytest>=9.0.3\", \"mypy\", \"ruff\"]\n```\n\n---\n\n## The Import Boundary\n\n```\nVireon (runtime)                    vireon-lab (educational)\n     │                                      │\n     │  ── pip install vireon-lab ──→        │\n     │                                      │\n     │  imports:                             │\n     │    from vireon import (sdk,           │\n     │      runtime, services)               │\n     │                                      │\n     │  NEVER imports:                       │\n     │    from vireon_lab import ...          │\n     │                                      │\n     ▼                                      ▼\n```\n\n**Rule**: `vireon` never imports from `vireon_lab`. `vireon_lab` imports from `vireon`. This is a one-way dependency. Circular imports are a build failure.